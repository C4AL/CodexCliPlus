# Cli Proxy API Desktop 待整合资产清单

本文件只保留当前主线真正相关的资产来源。已经过时、当前并不进入主线的设想不再继续堆在这里。

## 当前主线资产

### 1. 官方主程序基线

- 仓库：`router-for-me/CLIProxyAPI`
- 当前作用：作为 CPAD 兼容收口的官方主程序基线
- 当前状态：已可同步到安装目录 `upstream\CLIProxyAPI`
- 后续动作：对比 `CPA-UV` 覆盖差异，产出兼容矩阵

### 2. 官方管理中心基线

- 仓库：`router-for-me/Cli-Proxy-API-Management-Center`
- 当前作用：作为管理中心能力拆解的官方来源
- 当前状态：已可同步到安装目录 `upstream\Cli-Proxy-API-Management-Center`
- 后续动作：拆分出应进入 CPAD 的前后端能力，避免整仓并入

### 3. CPA-UV 源仓

- 路径：`C:\Users\Reol\workspace\CPA-UV-publish`
- 当前作用：提供现有覆盖能力、运行时行为与兼容逻辑来源
- 当前状态：作为 CPAD 的受控外部源仓保留，不直接复制进主仓
- 后续动作：梳理相对官方主程序基线的覆盖项与保留项

### 4. 自有插件源仓

- 路径：`C:\Users\Reol\workspace\omni-bot-plugins-oss`
- 当前作用：提供插件市场清单、插件安装源与版本来源
- 当前状态：已接入插件市场刷新、安装、更新、启用、禁用和诊断流程
- 后续动作：补充版本策略、插件兼容标记与发布流程

## 当前不直接并入主仓的资产

- 历史脚本中的安装与同步逻辑
- 历史脚本中的本地 / 官方模式切换逻辑
- 旧桌面快捷方式、旧桥接目录和临时迁移脚本

处理原则：

- 能抽职责的先抽职责
- 能删除的一次性脚本不要长期背在主仓里
- 只迁入对 CPAD 主链路有明确价值的能力

## 暂不纳入当前主线

以下方向不作为当前 `M5/M6` 主线任务：

- 泛化第三方插件生态
- 非 Windows 平台支持
- 多语言界面
- 与 CPAD 主链路无关的桥接项目或实验性工具仓

## 整合原则

- 先抽职责，再迁代码
- 先统一产品边界，再统一技术栈
- 先保留历史来源记录，再执行删除或重写
- 任何整合动作都必须遵守点对点同步和单一真实数据源原则
