# Cli Proxy API Desktop 待整合资产清单

以下项目已被视为后续需要按价值拆解、筛选和并入的新项目资产来源。

## 计划归档并后续按需吸收

- `CPA-UV`
- `Cli-Proxy-API-Management-Center`
- `codex-edge-control-bridge`
- `cpa-codex-quota-bridge`

## 已确认的本机相关资产来源

- 当前运行中的 `CPA-UV` 安装与同步脚本逻辑
- 当前本机 `codex` 本地 / 官方模式切换脚本逻辑
- 现有自有插件集合
- 现有插件市场索引格式

## 后续整合原则

- 先抽职责，再迁代码
- 先统一产品边界，再统一技术栈
- 先保留历史来源记录，再执行删除或重写
- 只迁入对桌面主链路有价值的能力
