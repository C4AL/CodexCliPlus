Bundled plugin source snapshots synchronized into the desktop package.
These directories seed the plugin market catalog.
Managed installed copies are mirrored into ../plugins at runtime.
See cpad-plugin-source-bundle.json for the packaged source list.
