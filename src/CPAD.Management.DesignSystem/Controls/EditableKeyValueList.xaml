<UserControl x:Class="CPAD.Management.DesignSystem.Controls.EditableKeyValueList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <StackPanel>
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource ManagementSecondaryTextBrush}"
                       Text="{Binding KeyHeader, ElementName=Root}" />
            <TextBlock Grid.Column="1"
                       Margin="8,0,0,0"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource ManagementSecondaryTextBrush}"
                       Text="{Binding ValueHeader, ElementName=Root}" />
        </Grid>

        <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=Root}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox Grid.Column="1"
                                 Margin="8,0,0,0"
                                 Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Grid.Column="2"
                                Margin="8,0,0,0"
                                Click="RemoveButton_Click"
                                Content="删除"
                                Tag="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Margin="0,4,0,0"
                HorizontalAlignment="Left"
                Click="AddButton_Click"
                Content="{Binding AddButtonText, ElementName=Root}" />
    </StackPanel>
</UserControl>
