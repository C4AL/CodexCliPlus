<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:CPAD.Management.DesignSystem.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CPAD.Management.DesignSystem;component/Themes/DesignTokens.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ManagementPageTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource ManagementPrimaryTextBrush}" />
    </Style>

    <Style x:Key="ManagementPageSubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource ManagementSecondaryTextBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="ManagementCardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ManagementSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ManagementBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource ManagementCardCornerRadius}" />
        <Setter Property="Padding" Value="{StaticResource ManagementCardPadding}" />
        <Setter Property="Margin" Value="{StaticResource ManagementSectionSpacing}" />
        <Setter Property="Effect" Value="{StaticResource ManagementSoftShadow}" />
    </Style>

    <Style x:Key="ManagementSectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource ManagementPrimaryTextBrush}" />
    </Style>

    <Style x:Key="ManagementHintTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource ManagementSecondaryTextBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="BorderBrush" Value="{StaticResource ManagementBorderBrush}" />
        <Setter Property="Background" Value="{StaticResource ManagementSurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ManagementPrimaryTextBrush}" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="BorderBrush" Value="{StaticResource ManagementBorderBrush}" />
        <Setter Property="Background" Value="{StaticResource ManagementSurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ManagementPrimaryTextBrush}" />
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="ListView">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="Background" Value="{StaticResource ManagementSurfaceSubtleBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ManagementPrimaryTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ManagementBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="{x:Type controls:ManagementTabs}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="Foreground" Value="{StaticResource ManagementPrimaryTextBrush}" />
    </Style>
</ResourceDictionary>
