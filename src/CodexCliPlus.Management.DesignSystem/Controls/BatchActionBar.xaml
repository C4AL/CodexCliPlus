<UserControl x:Class="CodexCliPlus.Management.DesignSystem.Controls.BatchActionBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Style="{StaticResource ManagementCardBorderStyle}"
            Background="{StaticResource ManagementSurfaceAccentBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource ManagementAccentStrongBrush}"
                       Text="{Binding Summary, ElementName=Root}" />
            <ContentPresenter Grid.Column="1"
                              Content="{Binding Actions, ElementName=Root}"
                              VerticalAlignment="Center" />
        </Grid>
    </Border>
</UserControl>
