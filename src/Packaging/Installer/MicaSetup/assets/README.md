# Asset Placeholders

If BuildTool starts rendering icons or bitmap assets into `micasetup.json`, keep the repo-owned files here.

Suggested file names:

- `Favicon.png`
- `FaviconSetup.png`
- `FaviconUninst.png`

The current overlay ships templates only, so these files are intentionally not populated yet.
